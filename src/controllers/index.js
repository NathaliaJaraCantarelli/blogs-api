const createUserController = require('./login.controller');
const createCategoryController = require('./category.controller');

module.exports = {
    createUserController,
    createCategoryController,
};
